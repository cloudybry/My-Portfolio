node_modules/
dist/
# PocketBase binary (if compiled locally)
pocketbase

# Shell script (if it's machine-specific or temporary)
start.sh

# Backup folder (often contains sensitive or large files)
backups/

# Dockerfile (only if you're not using Docker at all)
Dockerfile
